{% load staticfiles %}

<link rel="stylesheet" type="text/css" href="{% static 'books/style.css' %}" />{% if books_list %}
    <ul>
    {% for books in books_list %}
        <li><a href="{% url 'books:detail' books.id %}">{{ books.id }}</a></li>
    {% endfor %}
    </ul>
{% else %}
    <p>No polls are available.</p>
{% endif %}

<h1>Index books</h1>
