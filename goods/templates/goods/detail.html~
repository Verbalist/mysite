{% load staticfiles %}

<script src = "{% static "goods/jquery-2.1.1.min.js"%}"></script>
<script src="{% static "goods/action.js"%}"></script>
<h1>Detail Goods</h1>
<h1>{{ goods.name }}</h1>
<h2>{{ goods.base }}</h2>
<h2>{{ goods.technic }}</h2>
<h2>{{ goods.price }}</h2>
{{ goods.description|safe }}
<h2>{{ goods.color }}</h2>
<h2>{{ goods.age }}</h2>
<h2>{{ goods.consumer }}</h2>
<h2>{{ goods.pub_date }}</h2>
<h2>{{ goods.count }}</h2>
<h2>{{ goods.style }}</h2>
{% if error_message %}<p><strong>{{ error_message }}</strong></p>{% endif %}

<script src="{% static "goods/action.js"%}"></script>
