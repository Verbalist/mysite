{% load staticfiles %}

<link rel="stylesheet" type="text/css" href="{% static 'contacts/style.css' %}" />{% if contacts_list %}
    <ul>
    {% for contacts in contacts_list %}
        <li><a href="{% url 'contacts:detail' contacts.id %}">{{ contacts.contacts_text }}</a></li>
    {% endfor %}
    </ul>
{% else %}
    <p>No polls are available.</p>
{% endif %}

<h1>Index</h1>
